# npm integrity hashes and unit-test fixtures are not live credentials.
# Real ArenaNet keys are stored encrypted in Mongo and never committed.
paths-ignore:
  - 'package-lock.json'
  - '**/*.test.ts'
